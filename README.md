# Test MQTT + React

## Setup

- Install `nodejs`
  - Check `node -v`
- In terminal
  - `npm i -g pnpm`
  - Check `pnpm -v`

## Get code

- Download from zip

## VSCode

- `pnpm install`
- Copy `.env.example` to `.env`
  - Input information
- `pnpm run dev`
